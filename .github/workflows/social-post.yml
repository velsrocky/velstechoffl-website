name: Post new articles to socials

on:
  push:
    branches: [main]
    paths:
      - "articles.js"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  post:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Detect and post new articles
        run: node tools/post-new-articles.js
        env:
          # X / Twitter (API v2) – set in repo Settings → Secrets and variables → Actions
          TWITTER_API_KEY: ${{ secrets.TWITTER_API_KEY }}
          TWITTER_API_SECRET: ${{ secrets.TWITTER_API_SECRET }}
          TWITTER_ACCESS_TOKEN: ${{ secrets.TWITTER_ACCESS_TOKEN }}
          TWITTER_ACCESS_SECRET: ${{ secrets.TWITTER_ACCESS_SECRET }}
          # Mastodon
          MASTODON_INSTANCE: ${{ secrets.MASTODON_INSTANCE }}
          MASTODON_TOKEN: ${{ secrets.MASTODON_TOKEN }}
          # Generic webhook (n8n / Zapier / Buffer / Make)
          WEBHOOK_URL: ${{ secrets.WEBHOOK_URL }}
